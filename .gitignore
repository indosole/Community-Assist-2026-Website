.DS_Store

# Build output / dependencies
node_modules/
dist/
.astro/

# Heavy / stray source files that must NOT ship to the public web root.
# (The page references the optimised public/assets/img/*.webp, never these.)
public/assets/img/hero-people.png
Community Assist Carousel.mov
image.png
image copy*.png

# Build-phase reference material — kept locally, purged from the repo for client handoff.
# (Removed from history too; restore from a backup bundle if ever needed.)
# Note: HANDOFF.md is intentionally tracked — it's the maintainer/editing runbook.
COMMUNITYASSIST__2026.pdf
CLAUDE.md
BUILD-NOTES.md
docs/
